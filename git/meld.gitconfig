[merge]
	tool = meld
[diff]
	tool = meld
[mergetool]
	prompt = false
	# no <file>.orig after every resolve; the merge is in the index already
	keepBackup = false
[mergetool "meld"]
	# settle the hunks that do not really conflict, leaving only the decisions
	useAutoMerge = true
