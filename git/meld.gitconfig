[merge]
	tool = meld
[diff]
	tool = meld
[mergetool]
	prompt = false
[mergetool "meld"]
	# Let meld settle the hunks that do not actually conflict, so the panes
	# hold the decisions you have to make and not much else. git's own
	# default hands you the whole file.
	useAutoMerge = true
